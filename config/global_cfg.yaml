MULTIPROCESSING:
  ENABLED: true   
  NUM_WORKERS: 4       

PREDICTION:
  BATCH_SIZE: 16        

# Device Configuration
# Support Device: ['WEBGPU', 'REMOTE', 'QCOM', 'PYTHON', 'NV', 'NULL', 'NPY', 
#                 'METAL', 'LLVM', 'HIP', 'GPU', 'DSP', 'DISK', 'CUDA', 'CPU', 'AMD']
DEVICE:  CUDA:0  # If a default value needs to be retained, keep "Default".
